import React from 'react';

const JobCategory = ({jobCategory}) => {
    
    // const {name} = jobCategory;
  return (
    <div>
      {/* <h4>{name}</h4> */}
      
      {/* Add any other properties of jobCategory that you want to display */}
    </div>
  );
};

export default JobCategory;
